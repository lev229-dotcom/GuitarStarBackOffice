@using GuitarStarBackOffice.Shared
@layout MainLayout
@inject Radzen.DialogService dialogService

<RadzenContent Container="main">

    <ChildContent>
        <div class="row">
            <div class="col-md-12">
                <RadzenTemplateForm TItem="Shipment" Data="@newShipment" Submit="@HandleAdd">
                    <ChildContent>
                        <div style="margin-bottom: 1rem" class="flex-column">
                            <RadzenLabel Text="Дата поставки" style="width: 100%" />
                            <RadzenDatePicker  @bind-Value="@newShipment.ShipmentDate" Inline="true" />
                        </div>

                        <div style="margin-bottom: 1rem" class="flex-column">
                            <RadzenLabel Text="Поставщик" style="width: 100%" />
                            <RadzenDropDown Data="@suppliers" @bind-Value="@(newShipment.SupplierId)"
                                            SelectedItem="1"
                                            ValueProperty="IdSupplier" TextProperty="SupplierName" />
                        </div>

                        <div class="row footer">
                            <RadzenButton ButtonType="Radzen.ButtonType.Submit" Icon="save" ButtonStyle="ButtonStyle.Primary" Text="Сохранить">

                            </RadzenButton>
                        </div>
                    </ChildContent>
                </RadzenTemplateForm>
            </div>
        </div>
    </ChildContent>
</RadzenContent>